@model BitacoraDireccionesIp.Models.tb_bit_cat_area

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>tb_bit_cat_area</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.pk_cve_area)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.pk_cve_area)
            @Html.ValidationMessageFor(model => model.pk_cve_area)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.des_area)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.des_area)
            @Html.ValidationMessageFor(model => model.des_area)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
